import React from 'react';
import ReactDOM from 'react-dom/client';
import MinistryPlatform from './MinistryPlatform';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <MinistryPlatform />
  </React.StrictMode>
);
